/*
import * as mongoose from "mongoose";
import {
    createNewUser,
    deleteExistingUser,
    doesUserExist,
    UserInterface,
    UserQueryInterface,
} from "../../src/data";

describe("manipulating user data", () => {
    test("check if user exists", async () => {
        const userData: UserQueryInterface = {
            username: "smordie0",
        };

        expect(await doesUserExist(userData)).toBe(false);
    });

    /*
    test("successfully create user", () => {
        const dora: UserInterface = {
            username: "doraTheExplorer",
            email: "dora@thejungle.com",
            password: "8398kdkf9s2!",
        };

        createNewUser(dora);
    });
    */
});
*/
